{
  "name": "betteryourads",
  "version": "1.0.0",
  "private": true,
  "description": "BetterYourAds — one Next.js app (App Router): the SPA as a React client, backend as route handlers, prompts isolated in lib/prompts.ts.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "setup": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.1",
    "next": "^15.1.6",
    "playwright": "^1.48.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "typescript": "^5.7.3"
  }
}
